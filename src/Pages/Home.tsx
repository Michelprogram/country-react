import React from 'react';
import Navigation from '../Components/Navigation';

const Home = () => {
    return (
        <div>
            <Navigation />

            <h1>Accueil</h1>
        </div>
    );
};

export default Home;